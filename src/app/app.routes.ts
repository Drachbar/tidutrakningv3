import { Routes } from '@angular/router';
import {TidrapporteringComponent} from './tidrapportering/tidrapportering.component';

export const routes: Routes = [
  {path: 'utrakning', component: TidrapporteringComponent }
];
